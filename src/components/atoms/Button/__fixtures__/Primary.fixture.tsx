/**
 * Cosmos fixture: Button → Primary
 * ----------------------------------------------------------------------------
 * A *fixture* is a file that exports one or more "scenarios" for a component.
 * Cosmos auto-discovers every *.fixture.* file in the project and shows
 * them in the left navigation tree.
 *
 * Naming convention:
 *   The Cosmos tree groups fixtures by their parent folder, e.g.:
 *     atoms/Button/__fixtures__/Primary.fixture.jsx
 *   appears under the tree path "atoms / Button / Primary".
 */
import Button from '../Button';

export default <Button variant="primary">Adopt me</Button>;
